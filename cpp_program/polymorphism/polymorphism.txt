C++多态:
静态多态，也即是编译时多态，指的是在一个类中的多个重载函数，在编译时根据函数调用的参数个数与类型的不同，而调用不同的函数实体。
动态多态，也即是运行时多态，指一个函数在运行时被调用，会根据当前调用对象的虚函数表寻找对应的函数实体进行运行。

1 一个空类的大小是1byte，一个空虚类的大小是指针大小，一个多重继承虚多个虚基类的类的大小是继承虚基类个数乘指针大小。
2 如果多重继承多个虚基类，并且虚基类中有相同的虚函数，那么子类重写现这个相同的虚函数，会将多个虚基类的虚函数都重写。